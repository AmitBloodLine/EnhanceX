import React from 'react'
import ReactDOM from 'react-dom/client'
import GestureRecognition from './Components/Gesture-recognition.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GestureRecognition />
  </React.StrictMode>,
)
